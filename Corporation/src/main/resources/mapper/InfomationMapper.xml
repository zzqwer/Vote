<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yc.corporation.mapper.InfomationMapper">
	<!-- 查找活动预告信息 -->
	<select id="findannounced" resultType="Infomation">
		select * from infomation where infotype='activeannounced'
	</select>
	<!-- 根据id查找活动预告内容 -->
	<select id="findactivebyid" parameterType="int" resultType="Infomation">
		select * from infomation where infotype='activeannounced' and
		infoid=#{infoid}
	</select>
	<!-- 查找网站公告内容 -->
	<select id="findwebnotice" resultType="Infomation">
	select * from (select rownum rn,a.* from(select *from infomation where infomation.infotype='webnotice' order by infodate desc )a where rownum  &lt;= 7 ) where rn>0
	</select>
	
	<!-- 查找社团动态内容 -->
	<select id="finddongtai" resultType="Infomation">
	select * from (select rownum rn,a.* from(select *from infomation where infomation.infotype='shetuandongtai' order by infodate desc )a where rownum  &lt;= 7 ) where rn>0
	</select>
	
	<!-- 查找社团荣誉内容 -->
	<select id="findglory" resultType="Infomation">
	select * from (select rownum rn,a.* from(select *from infomation where infomation.infotype='glory' order by infodate desc )a where rownum  &lt;= 7 ) where rn>0
	</select>
	
	<!-- 查找对外交流内容 -->
	<select id="finddiscuss" resultType="Infomation">
	select * from (select rownum rn,a.* from(select *from infomation where infomation.infotype='discuss' order by infodate desc )a where rownum  &lt;= 7 ) where rn>0
	</select>
	
	<!-- 查找精品活动内容 -->
	<select id="findboutique" resultType="Infomation">
	select * from (select rownum rn,a.* from(select *from infomation where infomation.infotype='Boutique' order by infodate desc )a where rownum  &lt;= 7 ) where rn>0
	</select>

</mapper>